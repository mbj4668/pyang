  o  handle core yang stmts in extesions
     - coding done, write test cases

  o  add validation of XPath (lower prio) (when, must)

  o  add more stuff to XSD output:
     o  generate xs:unique for 'unique'

  o  handle relative inline augment

  o  add validation of instance-identifier defaults

  o  rewrite .validate to be side-effect free

  o  better handling of multiple input files

  o  make the parsers even more agnostic of yang grammar - they should
     not even know about Module.  idea: pass an optional handle
     function to the parser which can do the classes.  the same parser
     can then be used to implement overlays.

  o  restructure statements.py
     - get rid of duplicate syntax checks
     - remove most special classes?
