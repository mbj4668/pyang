  o  add validation of XPath (lower prio) (when, must)

  o  add more stuff to XSD output:
     o  generate xs:unique for 'unique'

  o  handle relative inline augment

  o  add validation of instance-identifier defaults

  o  better handling of multiple input files

BEFORE NEXT RELEASE:

  o  handle local extensions defined in a submodule

  o  update to -02:
     - features
     - deviations
     - belongs-to prefix (rewrite yin_parser which parses the module)
